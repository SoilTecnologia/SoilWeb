import styled, {css} from "styled-components";
export const Container =styled.div` ${({theme})=> css` `} `;
export const PercentChartContainer=styled.div`
  margin:20px;
  display:flex;
  width:50% ;
  align-self:flex-start;
`
